{
  "watch": [
    "server/**/*.ts",
    "src/**/*.graphql"
  ],
  "exec": "ts-node -r tsconfig-paths/register --project tsconfig.build.json server/index.ts",
  "env": {
    "NODE_ENV": "development"
  },
  "ext": "ts graphql"
}
